#!/bin/sh

if [ -z "$DISPLAY" ]; then
    weston-launch -- --orbital-client=@CMAKE_INSTALL_PREFIX@/libexec/startorbital
else
    weston --orbital-client=@CMAKE_INSTALL_PREFIX@/libexec/startorbital
fi
